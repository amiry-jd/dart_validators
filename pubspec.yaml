name: dart_validators
description: A useful set of validators for using in an AngularDart web application.
version: 0.0.1
homepage: https://github.com/javad-amiry/dart_validators
author: javad amiry <javad.d.amiry@gmail.com>

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:
  angular: ^4.0.0
  angular_forms: ^1.0.0

dev_dependencies:
  test: ^0.12.0
